# ════════════════════════════════════════════════════
#  Rustace Bot — Environment Configuration
#  Copy this file to .env and fill in the values.
#  https://github.com/ankit-chaubey/RustaceBot
# ════════════════════════════════════════════════════

# ── Required ─────────────────────────────────────────
# Your bot token from @BotFather
BOT_TOKEN=1234567890:AAXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Runtime Mode ─────────────────────────────────────
# Choose between "polling" (default) or "webhook"
# polling  → long-poll (recommended for development)
# webhook  → HTTPS server (recommended for production)
BOT_MODE=polling

# ── Polling Options (only used when BOT_MODE=polling) ─
POLLING_TIMEOUT=30
POLLING_LIMIT=100

# ── Webhook Options (only used when BOT_MODE=webhook) ──
# Your public HTTPS base URL (no trailing slash)
WEBHOOK_URL=https://your-domain.com
# Local port to listen on (Telegram supports 80, 88, 443, 8443)
WEBHOOK_PORT=8080
# URL path that Telegram will POST to
WEBHOOK_PATH=/webhook
# Optional secret token for request validation (recommended!)
WEBHOOK_SECRET=change_me_to_a_random_string
# Maximum simultaneous connections (1–100)
WEBHOOK_MAX_CONNECTIONS=40
# Drop pending updates when starting (true/false)
WEBHOOK_DROP_PENDING=false

# ── Logging ───────────────────────────────────────────
# Rust log level: error | warn | info | debug | trace
RUST_LOG=info

# ── Custom API Server (optional) ──────────────────────
# If you run a local Telegram Bot API server, set the URL here.
# Leave blank to use the default https://api.telegram.org
# TELEGRAM_API_URL=https://api.telegram.org

# ── Bot Admin ─────────────────────────────────────────
# Your personal Telegram user ID (for admin-only commands)
ADMIN_ID=123456789
